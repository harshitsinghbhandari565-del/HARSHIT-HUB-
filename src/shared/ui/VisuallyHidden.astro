---
/**
 * VisuallyHidden — hides content visually while keeping it available to
 * screen readers (shared `.visually-hidden` utility). Use for labels,
 * announcer text, and honeypot-adjacent helpers — never for information
 * sighted users need.
 */
---

<span class="visually-hidden"><slot /></span>
