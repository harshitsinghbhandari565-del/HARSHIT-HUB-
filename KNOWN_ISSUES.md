# Known Issues

Every discovered concern is recorded here — never silently ignored.

## Resolved (kept for history)

| # | Item | Resolution |
|---|---|---|
| TD-7 | Fonts not self-hosted | **Resolved in Phase 1** — Astro Fonts API local provider; hashed self-hosted woff2, latin subset, swap, metric-adjusted fallbacks, first-paint preloads only (D-016) |
| TD-9 | `astro check` hint in eslint.config.js | **Resolved in Phase 1** — migrated to ESLint core `defineConfig()`; typecheck now 0 errors / 0 warnings / 0 hints |
| TD-1 | Temporary index.astro placeholder shell | **Resolved in Phase 4** — homepage assembly replaced the shell (TD-1 closed in the Phase E commit) |
| TD-4 | robots.txt not environment-aware | **Resolved in Phase 7** — `scripts/robots.mjs` derives `dist/robots.txt` from `public/robots.txt`; Netlify previews/branch deploys rewritten to `Disallow: /` via CONTEXT; production policy validated + smoke-tested; wired into the Netlify build command |
| TD-5 | CSP header not shipped | **Resolved in Phase 7** — strict hash-union CSP enforced via netlify.toml, generated by `scripts/generate-csp.mjs` with CI drift check (D-042) |
| TD-6 | View Transitions focus management missing (TAD §15.4) | **Resolved in Phase 7** — `astro:page-load` handler moves focus to the new h1/main and announces the title via a polite live region; initial load untouched (D-044, lib/navA11y.ts) |
| TD-14 | Contact effective JS ~16.8 KB vs 15 KB budget | **Resolved in Phase 7 (D-043)** — under the TD-13 method (eager = what loads at page load) /contact is 7.24 KB ≤ 15 KB; `client:visible` hydration happens on scroll and is reported separately by the budget gate. No budget revision needed |
| FI-1 | ADRs not migrated into docs/adr/ | **Resolved in Phase 7** — ADR-0001…0012 migrated verbatim from TAD §23 into individual files; TAD remains canonical |
| — | Vitest unhandled errors in search-mount suite (deferred Preact work after teardown; fails the run) | **Resolved in Phase 7** — tests settle observable markers before teardown + afterEach timer flush; 0 unhandled errors |
| — | ClientRouter navigations broke chrome listeners + stale toggle state (Phase 7 audit) | **Resolved in Phase 7 (D-044)** — document-level delegation + `astro:after-swap` re-sync |
| — | Gallery heading-order axe violation (h1 → h3) | **Resolved in Phase 7 (D-046)** — PresentationCard `headingLevel` prop |
| — | TAD §17.2/§17.3 unimplemented (no canonical/OG/Twitter metadata, no JSON-LD) — found by the Phase 7 compliance audit | **Resolved in Phase 7 (D-047)** — SeoHead + jsonLd builders + integration suite; og:image awaits the IA-2 brand asset |

## Current Issues

| # | Item | Status |
|---|---|---|
| CI-1 | **T-D7 projector dry-run pending** — the M3 manual gate needs Harshit's real Slides/Dropbox URLs (IA-2) and physical panel access; everything automatable around it is test-verified (D-031) | Open — blocked on content + hardware |
| CI-2 | **Mock seed content live** — three labeled mock presentations (D-029) render until Harshit's real decks replace them | Open — replacement is a content-only change |
| CI-3 | **Schema `order` field is unused** — TAD §8.1 defines `order` on presentations; sorting currently uses date/subject only. Accepted by the schema, documented as not consumed (ADDING-A-PRESENTATION.md) | Open — implement when a use case appears (or drop from schema via a future TAD revision) |
| CI-4 | **Benign CSP violation on every client-side navigation** — ClientRouter inserts one empty `data:`-URL probe script (timing heuristic); strict CSP blocks it. No functional impact (nothing consumes it); `data:` deliberately not allowlisted (XSS vector). Violation noise only; disappears if/when Astro changes the router | Open — accepted, documented (D-042) |
| CI-5 | **Astro 6 pinned-stack advisories** — three astro XSS advisories (no reachable attack surface in this codebase: no transition:* directives, only constant attribute spreads, repo-authored content) + sharp/libvips CVEs (only repo-authored images processed). Allowlisted with rationales in `scripts/audit.mjs` (D-045); gate fails on stale entries | Open — exit ramp is the Astro 6→7 upgrade (ADR-level decision) |

_All quality gates green at Phase 7 completion: typecheck 0/0/0, ESLint 0, Stylelint 0, tests 250/250 (32 files, incl. 24 site-wide axe scans + 11 SEO integration checks), build clean (12 pages + search index), budgets within TAD §14.1 on every route, CSP + robots + link health + audit gates green._

## Technical Debt

| # | Item | Origin | Resolution path |
|---|---|---|---|
| TD-2 | Dark palette is **derived** (Design v1 §24.2 values not delivered); jsdom cannot compute rendered contrast, so the both-themes contrast audit is a **real-browser manual pass** | D-005 / assumption B1 | Replace verbatim if Design v1 arrives; manual contrast verification on the first deploy preview (RELEASE_CHECKLIST) |
| TD-3 | Stylelint covers standalone CSS only — not `.astro` scoped styles; strict-value plugin has no shorthand expansion | D-013 | Custom-syntax integration + review discipline (see FI-10) |
| TD-8 | Placeholder favicon | Initialization | Replace when design provides the brand mark |
| TD-10 | Skeleton/loading detailed spec lives in Design v1 (not delivered); current Skeleton is a conservative, token-driven placeholder with documented semantics | Phase 1 (D-018 era) | Replace/extend when Design v1 loading-state spec arrives |
| TD-11 | Tablet "More" `<details>` dropdown does not auto-close on outside click or Escape (native disclosure behavior; D-020) | Phase 2 | If review deems it needed, add a small shared script before launch |
| TD-12 | Homepage copy constants are labelled MOCK (tagline, about/contact teasers — D-034) and the About teaser visual is a gradient placeholder pending the profile image | Phase 4 | Content-only replacements when Harshit's words/image arrive (IA-2) |
| TD-13 | Budget measurement method note — **now automated**: `scripts/budgets.mjs` implements the TD-13 standard as the build gate (D-043); earlier manual numbers under-reported transitive island deps until D-038 corrected them | Phase 5 | Method is codified; keep the gate authoritative |
| TD-15 | Astro checker gotcha: a `<` character inside an Astro frontmatter comment breaks Props typing for that file (found via MobileMenu) | Phase 7 | Avoid `<` in frontmatter comments (documented in RUNBOOK §8 triage knowledge); upstream issue if reproducible minimally |

## Future Improvements

| # | Item | Phase |
|---|---|---|
| FI-2 | Playwright E2E (8 journeys) in CI — the Phase H axe-both-themes half is DONE (`tests/a11y/`); browser E2E deferred (deploy/hardware-dependent; jsdom journey suites + manual checklist cover the interim) | Post-launch |
| FI-3 | Lighthouse CI per-route budgets on deploy previews — build-time budget gate DONE (`scripts/budgets.mjs`, TAD §14.1); lab Lighthouse needs deploy previews | Post-launch |
| FI-4 | External link liveness + `linkHealth` write-back + GitHub issues (ADR-0006 full vision) — internal link health DONE (script + weekly workflow); external liveness meaningless on mock URLs | After IA-2 content |
| FI-5 | Prefetch tuning (`data-astro-prefetch="viewport"` on rail cards) | Phase E era |
| FI-6 | Subject filter chips on the gallery | At 25–50 items (TAD §20.2) |
| FI-7 | Search migration (Pagefind/MiniSearch) | At ~100 items (TAD §20.2) |
| FI-8 | Decap/TinaCMS over the same Git files | If GitHub-web-UI authoring creates friction (ADR-0004) |
| FI-9 | Fonts config is the first thing to re-verify on Astro upgrades (local-provider `options.variants` shape, `<Font />` preload filter) | Any Astro minor/major bump (D-016) |
| FI-10 | Stylelint coverage for `.astro` scoped styles (custom syntax) and shorthand color expansion | When component CSS grows (TD from D-013) |
| FI-11 | Astro 6 → 7 upgrade (clears CI-5 advisories, newer CSP/router APIs) — requires re-verifying Fonts API (FI-9), content layer paths, and ClientRouter/CSP interplay | ADR-level decision, post-launch |

## Nice-to-Have Enhancements

| # | Item | Trigger |
|---|---|---|
| NH-1 | QR code component in the contact teaser | Phase 2 (PRD Could-have) |
| NH-2 | Privacy-respecting analytics (cookieless RUM) | Phase 2 (PRD A-8) |
| NH-3 | Service worker / offline support | Only if classroom network failures actually occur (TAD §20.4) |
| NH-4 | Visual regression testing | When the design system exceeds ~30 components (TAD §20.4) |
