# TAD §17.4 — production policy. Preview/branch deploys are rewritten to
# "Disallow: /" at build time by scripts/robots.mjs (TD-4).
# The Sitemap line tracks the canonical site URL (astro.config.mjs); when
# the custom domain lands (B7), update it there and here.
User-agent: *
Allow: /

Sitemap: https://harshit-portfolio-hub.netlify.app/sitemap-index.xml
